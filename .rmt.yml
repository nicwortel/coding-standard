vcs:
  name: git
  sign-tag: true

prerequisites:
  - working-copy-check
  - display-last-changes

version-generator: semantic

version-persister:
  vcs-tag:
    tag-prefix: 'v'

post-release-actions:
  vcs-publish:
    ask-confirmation: true
